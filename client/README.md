# Eligix Client (React + Vite)

This is a small React app (Vite) that talks to the backend API at `/api/*`.

Dev:
```bash
cd client
npm install
npm run dev
```

Build & serve (server will prefer `client/dist`):
```bash
cd client
npm install
npm run build
# then start the root server: from repo root `npm start`
```
